# Toonflow 用户侧项目治理边界

## 1. 身份与目标

本仓库由用户作为项目所有者推进。所有自动化代理、编码代理和协作者都必须以“用户侧配置与内容维护者”的身份工作，不得默认充当 Toonflow 系统框架开发者。

本文件适用于整个仓库。子目录中的后续 `AGENTS.md` 只能收紧规则，不得放宽本文件对系统框架的只读限制。

核心原则：

1. 默认只读；只有本文件明确列入“用户定义层”的路径才允许写入。
2. 路径归属不明确时，一律按系统框架处理并保持只读。
3. 用户要求实现某个结果，不等于授权修改系统框架。
4. 优先通过用户定义、提示词、Skill、模型映射或自定义供应商扩展实现需求。
5. 禁止为了方便、修复报错、通过检查或完成任务而顺手修改框架文件。

## 2. 仓库分层结论

### 2.1 用户定义层：允许按任务修改

以下是应用明确提供读取或编辑能力的文件化定制入口。

| 路径 | 用途 | 操作规则 |
| --- | --- | --- |
| `data/skills/**/*.md` | ScriptAgent、ProductionAgent、故事类型、画风、导演规则和提示词 Skill | 用户明确要求调整对应能力时可编辑；保持原目录结构、职责和引用关系 |
| `data/skills/**/*.{png,jpg,jpeg,webp,svg,gif,ico,bmp}` | Skill 配套参考图或图标 | 只有对应 Skill 任务明确需要时才可新增或替换 |
| `data/modelPrompt/image/**/*.md` | 图片模型提示词模板 | 可新增或编辑用户模板 |
| `data/modelPrompt/video/**/*.md` | 视频模型提示词模板 | 可新增或编辑用户模板；现有四个文件是官方初始模板，但属于产品设计的可编辑配置面 |
| `data/vendor/<custom-id>.ts` | 用户自定义模型供应商适配器 | 只允许新建或维护非官方 ID；推荐使用 `user-` 前缀 |
| `user/**/*.{md,json,yaml,yml,txt}` | 用户需求、路线图、决策记录、验收标准和项目推进文档 | 可按用户任务创建和维护；不得放置密钥或可执行代码 |
| `user/tools/**/*.mjs` | 用户侧 Toonflow 控制器与自动化工具 | 仅可调用 Toonflow 已支持的 HTTP / Socket API；不得直连数据库、写入密钥或修改框架 |
| `AGENTS.md` | 本治理边界 | 只有用户明确要求更新治理规则时才可修改 |

说明：`data/skills/` 中的内容随仓库发布，但 README 明确将核心提示词外化为可在线编辑的 Skill；因此它们是“系统提供初始值的用户定义层”，不是 TypeScript 系统框架。

### 2.2 官方供应商适配器：只读

以下文件是系统随版本维护的官方适配器，不属于用户自定义供应商。严禁直接修改、覆盖、改名或删除：

- `data/vendor/atlascloud.ts`
- `data/vendor/deepseek.ts`
- `data/vendor/grsai.ts`
- `data/vendor/klingai.ts`
- `data/vendor/minimax.ts`
- `data/vendor/null.ts`
- `data/vendor/openai.ts`
- `data/vendor/toonflow.ts`
- `data/vendor/vidu.ts`
- `data/vendor/volcengine.ts`
- `data/vendor/volcengineSd2.ts`

如果需要不同的供应商行为，应复制其接口约定并创建新的 `data/vendor/user-<name>.ts`，不得以修改官方文件的方式实现。官方升级逻辑可能自动覆盖部分内置适配器。

### 2.3 运行时用户数据：应用管理，代理不得手工修改

以下内容包含项目、账号、模型密钥、素材、日志或应用状态。它们不是框架源码，但也不是代理可直接编辑的文档层：

- `data/db2.sqlite`、`*.sqlite`、`*.sqlite-shm`、`*.sqlite-wal`
- `data/oss/`
- `logs/`
- `.env*`
- 应用导入、导出和上传产生的数据

规则：

1. 只允许通过 Toonflow 网页设置、受支持的 API 或用户明确指定的导入导出流程改变这些数据。
2. 禁止代理直接执行 SQL、手改数据库、清库、删除素材或读取并展示密钥。
3. 调试时可以只读检查存在性、大小、时间和脱敏后的错误信息。
4. 任何清理、迁移、导入、导出或覆盖操作都必须取得用户针对具体目标的明确授权。

### 2.4 系统框架层：严格只读

除第 2.1 节白名单外，仓库内其余内容均视为系统框架。包括但不限于：

- `src/**`：后端、Agent 调度、路由、中间件、数据库初始化、Socket、工具和类型系统
- `scripts/**`：构建、Electron 主进程、授权和打包脚本
- `data/serve/app.js`：已编译后端入口
- `data/web/**`：已编译网页前端
- `data/models/**`：本地模型及 tokenizer 文件
- `data/assets/**`：系统内置资产
- `src/lib/vendor.json` 及任何供应商快照/生成清单
- `build/**`、`dist/**`、`out/**`：生成产物
- `package.json`、`yarn.lock`、`tsconfig.json`、`nodemon.json`
- `Dockerfile`、`.dockerignore`、`electron-builder.yml`
- `.github/**`：CI/CD 和发布流程
- `.gitignore`、`.gitattributes`
- `README.md`、`docs/README.*.md` 及 `docs/**`：官方项目文档和资源
- `LICENSE`、`NOTICES.txt`：许可证、版权和第三方声明
- `src/router.ts`、`src/types/database.d.ts`：即使由程序生成，也属于框架文件，禁止手工修改
- `data/version.txt`：发布版本数据

对系统框架允许的行为仅限：读取、搜索、分析、引用、生成审计报告和查看 Git 差异。不得编辑、格式化、重命名、删除、移动、覆盖或自动修复。

## 3. 操作判定流程

每次准备写文件前，必须依次执行：

1. 列出计划修改的精确路径。
2. 对照第 2.1 节确认每个目标都在用户定义层白名单内。
3. 检查目标是否命中官方供应商名单、运行时数据或系统框架规则。
4. 若存在跨层依赖，只修改用户层；不得顺带修改框架调用方。
5. 若仅靠用户层无法实现，停止写入并向用户报告：
   - 期望结果；
   - 用户层尝试或可行替代；
   - 被阻挡的具体框架文件；
   - 若修改框架可能产生的风险。

不得以“改动很小”“只是配置”“只是生成文件”“为了兼容”“为了测试通过”为理由越过边界。

## 4. 框架变更例外

系统框架始终默认为只读。只有用户在当前任务中明确写出“授权修改系统框架”，并点名允许修改的文件或目录后，才能进入框架变更模式。

框架变更授权必须是本次、具体、限范围的授权，不得从以下内容推断：

- “帮我修好”“继续做”“全面优化”等宽泛表述；
- 之前任务中的授权；
- 对用户定义层的修改授权；
- 运行、构建或部署授权。

进入框架变更模式后仍必须：先说明影响面，保存无关用户改动，最小化补丁，运行适当检查，并在交付时逐项列出框架改动。未被点名的框架路径继续只读。

## 5. 用户定义层修改规范

### 5.1 Skill 文档

1. 先识别文件职责：决策层、执行层、监督层、故事类型、画风、导演规划或分镜提示词。
2. 只改与用户目标直接相关的 Skill，避免全库机械替换。
3. 保持现有 Markdown 层级、术语、工具名、字段名和输出格式。
4. 不虚构框架不存在的工具、API、数据库字段或路由。
5. 修改核心 Skill 时检查其被哪些 Agent 和二级/三级 Skill 引用，防止职责冲突。
6. README 位于 `data/skills/**` 时属于该 Skill 包说明，可随对应 Skill 一起维护；根目录和 `docs/` 下的 README 不可修改。

### 5.2 模型提示词模板

1. 仅使用 `image` 或 `video` 类型目录。
2. 文件名应稳定、可读，并避免覆盖无关模型模板。
3. 保持输入字段、输出格式、素材引用方式和模型能力边界一致。
4. 新增模板后说明预期绑定的模型、模式和用途；不得直接改数据库绑定关系。

### 5.3 自定义供应商

1. 新 ID 不得与官方供应商重名，推荐 `user-<provider>`。
2. 不得把 API Key、Token、Cookie 或账号写进 `.ts` 文件；密钥必须由应用设置页写入运行时配置。
3. 保持现有 `vendor`、`textRequest`、`imageRequest`、`videoRequest` 等导出契约。
4. 不得为适配自定义供应商而修改 `src/`、官方供应商或构建脚本。
5. 新增前先参考 `data/vendor/null.ts` 的接口模板，只读参考，不修改模板本身。

### 5.4 用户项目管理文档

项目推进材料统一放在 `user/`。建议使用：

- `user/PROJECT.md`：目标、范围与成功标准
- `user/ROADMAP.md`：阶段、里程碑和状态
- `user/DECISIONS.md`：重要决策及原因
- `user/ACCEPTANCE.md`：验收清单
- `user/CHANGELOG.md`：用户定义层变更记录

这些文件仅用于用户侧管理，不得伪装成框架配置，也不得引用未经实现的系统能力作为既成事实。

### 5.5 用户侧控制器

1. 可执行文件只允许放在 `user/tools/**/*.mjs`；其他 `user/` 路径仍仅允许文档和数据配置。
2. 只能通过 Toonflow 已支持的网页接口、HTTP API 或 Socket 事件读取和推进流程，不得直接访问 SQLite、`data/oss/` 或内部模块。
3. Toonflow 正在执行时，控制器只能监听事件、响应框架主动发起的工具回调和等待异步任务，不得发送新的对话指令。
4. 只有在完整对话轮次进入终态、流式消息静默且后台生成任务结束后，才可代表用户审批下一步。
5. 审批必须沿用 Toonflow 自身的评分、问题清单和建议方案，不得加入控制器自定义的质量标准或修复内容。
6. 首次审核为 B 级时，按 Toonflow 原生问题清单和建议方案完成一轮修复；修复后的复审达到 B 级即视为通过并继续下一阶段，不要求继续修复至 A 级。C、D 级仍按 Toonflow 原生建议修复和复审。
7. 登录密码、Token、Cookie 和供应商密钥只能通过环境变量或运行时交互提供，不得写入仓库文件、日志或配置示例。
8. 控制器遇到接口错误、消息状态不明确或缺少 Toonflow 原生建议时必须暂停并报告，不得猜测后继续写入运行时数据。
9. 每次执行或继续流程前，必须先检查 `http://localhost:10588`。若后端不可用，可自动启动仓库现有生产入口 `data/serve/app.js`，等待服务就绪后再连接控制器；后端已运行时不得重复启动。严禁为此运行构建命令或修改生产入口。
10. 自动启动本地后端只解决运行顺序，不代表获得向外部模型供应商发送项目内容的授权；Codex 环境需要外部网络或数据外发权限时，必须按实际供应商和数据范围取得用户明确授权。
11. 分镜表文本审核和修复阶段不生成分镜图。若 Toonflow 在该阶段以空 ID 列表调用 `generateStoryboard`，控制器必须明确跳过，不得调用图片生成接口，也不得将该空调用记录为生成失败；只有收到至少一个真实分镜 ID 后才可进入分镜图生成。

### 5.6 单集完整制作流程：导演计划到最终视频提示词

当用户要求“从导演计划开始”“执行本集完整流程”“继续本集”或对后续新一集提出等价要求时，默认执行本节流程。除非用户在当前任务中明确改变范围，否则单集流程的起点固定为阶段 1 导演规划，终点固定为视频工作台内全部轨道生成 Seedance 2.0 最终视频提示词。

**阶段 5 分镜面板写入完成不代表单集流程完成。只有阶段 5.5 的全部最终视频提示词生成并通过数据验收后，才可报告本集完成。**

#### 5.6.1 双轨职责

1. Toonflow 是执行轨：按其原生决策层、执行层、监督层、评分系统、问题清单和修复方案完成各阶段。
2. Codex/用户侧控制器是监控审批轨：识别节点输入输出、等待终态、按 Toonflow 原生结果代替用户审批，不改写 Toonflow 的任务内容和质量判断。
3. Toonflow 正在生成、流式输出或执行后台任务时，控制器必须保持静默；一个节点完整结束后才可作出一次审批。
4. 每个节点使用项目 ID、剧本 ID、当前阶段和工作区快照组成唯一判定依据；不同集、不同剧本不得复用上一集的完成状态或审批指纹。

#### 5.6.2 启动前检查

1. 确认目标 `projectId`、`scriptId`、剧本和基础资产均属于当前集。
2. 每个新分集开始前必须通过 Toonflow 支持的项目/分集列表接口重新解析目标分集，并把控制器配置绑定到该集的准确 `scriptId`；禁止默认沿用上一集的 `scriptId=1`、分集索引或最近一次上下文。
3. 确认视频模型为 Seedance 2.0，当前既定模式为纯文本多参模式；若模型或模式不符，暂停并报告，不得猜测替换。
4. 检查 Toonflow 后端 HTTP 服务、登录认证和 ProductionAgent Socket 均可连接。
5. 后端由 Codex 启动时，必须运行在允许访问已配置外部模型 API 的网络环境中。启动后先通过 Toonflow 自带模型测试验证“通用 AI”；出现 `EACCES`、`Cannot connect to API` 或同类错误时，停止流程并以具备外网权限的方式重启后端，不得提交批量任务碰运气。
6. 凭据只在环境变量或运行时交互中注入，不得写入配置、状态文件、命令日志或本文件。

#### 5.6.3 固定节点及输入输出

| 节点 | Toonflow 输入 | 必须观察到的输出与完成证据 | 控制器动作 |
| --- | --- | --- | --- |
| 阶段 1：导演规划 | 当前集剧本、现有资产 | `scriptPlan` 已写入且内容非空，工作区哈希发生对应变化，对话轮次和后台任务均进入终态 | 若 Toonflow 给出原生评分则按评分规则处理；通过后只审批一次并进入阶段 2 |
| 阶段 2：衍生资产分析 | `scriptPlan`、剧本、现有资产 | 衍生资产条目已写入，或 Toonflow 明确给出“无需衍生资产”的终态结论 | 沿用本次完整流程：有衍生资产时确认生成其资产图；清单为空时跳过阶段 3，进入阶段 4 |
| 阶段 3：衍生资产生成（按需） | 阶段 2 已确认的真实衍生资产 ID | Toonflow 的资产图片异步任务全部终止，所选资产状态均成功；任何失败项都必须保留并报告 | 等待全部后台任务结束后再审批；不得把“已提交”当作“已完成” |
| 阶段 4：构建分镜表 | 剧本、资产、`scriptPlan` | `storyboardTable` 已写入且内容非空，工作区哈希发生对应变化，Toonflow 原生监督评分已返回 | 完全依照 Toonflow 的评分、问题清单和修复方案执行第 5.6.4 节既定审批规则 |
| 阶段 5：纯文本多参分镜面板写入 | 阶段 4 分镜表、已存在资产、Seedance 2.0 多参模式 | 分镜逐行写入；每条具有非空 `videoDesc`、正确资产关联和时长；`shouldGenerateImage=0`；写入行数与分镜表一致 | 不调用分镜图生成。阶段 5 完成后立即进入阶段 5.5，不在此处宣布流程结束 |
| 阶段 5.5：最终视频提示词 | 视频工作台轨道的 `videoDesc`、关联资产、视频模型和多参模式 | 工作台每个轨道状态为已完成且 `prompt` 非空；完成数等于轨道总数；失败数和生成中数量均为 0 | 使用 Toonflow 支持的工作台接口生成并轮询；全部通过后写入 `finished` 并结束控制器 |

#### 5.6.4 审批与修复规则

1. 只根据 Socket 工具事件、完整对话终态、后台任务终态和工作区数据变化识别节点；“完成了”“已完成”等自然语言不能单独作为放行依据。
2. 同一节点、同一工作区快照、同一评分结果只允许审批一次；重复事件必须静默跳过。
3. A 级直接通过。首次审核为 B 级时，按 Toonflow 原生问题清单和建议方案修复一次；修复后复审仍为 B 级时直接通过，不再追求 A 级。C、D 级继续按 Toonflow 原生方案修复和复审。
4. 控制器不得自行增加质量标准、改写修复方案或代替 Toonflow 创作内容。非审核节点没有原生评分时，只按明确的输入输出完成证据审批。
5. 阶段 4 数据已变化但尚未收到 Toonflow 原生评分时必须等待，不得提前进入阶段 5。

#### 5.6.5 图片与视频边界

1. 阶段 3 可按衍生资产清单生成资产图，这是本完整流程的一部分。
2. 当前 Seedance 2.0 纯文本多参流程不执行阶段 6，不生成分镜图；阶段 5 的 `shouldGenerateImage` 必须全部为 `0`。
3. 阶段 5.5 只生成视频提示词，不调用视频生成接口，不生成视频。
4. “不生成分镜图”不得被解释为“跳过衍生资产图”，也不得被解释为“停止生成最终视频提示词”。

#### 5.6.6 中断、恢复与最终验收

1. 接口错误、Socket 断开、模型测试失败、状态不明确、后台生成失败或缺少原生修复建议时，立即暂停并报告；不得自动重复审批或从头重跑。
2. 恢复时先读取 `controller-state.json`，再通过 Toonflow 支持的接口核对当前工作区；只从未完成节点继续，不重复执行已经验收的导演规划、资产或分镜表。
3. 阶段 5.5 开始前若全部轨道已有非空提示词，则不得重复生成，可直接验收；若仍有缺失或失败项，才允许发起一轮工作台批量提示词生成并轮询到终态。
4. 单集完成必须同时满足：阶段 1 至阶段 5 的必要输出均存在；阶段 3 的已选资产图任务无失败；阶段 6 已按多参规则跳过；视频工作台轨道数大于 0；所有轨道提示词非空且状态成功；控制器持久化阶段为 `finished`、运行 PID 已清空。
5. 只有满足上一条全部条件后，才可向用户报告“从导演计划到最终视频提示词的完整流程已完成”。仅完成当前阶段、提交异步任务或生成部分提示词都不得视为整集完成。

#### 5.6.7 不间断推进与防误停

1. “不中断”指在没有真实外部故障、缺失原生建议或数据不明确时，控制器必须持续推进到阶段 5.5 验收完成；不得把自然语言中的“错误”“失败”“是否继续”等字样单独识别为接口错误、生成失败或人工阻塞。
2. 每个审核节点独立计算修复轮数。进入新阶段时必须清零上一阶段的修复计数，禁止把阶段 1 的修复轮数带入阶段 4。首次 B 修复后的 B 直接通过；C、D 即使达到控制器本地默认修复轮数，只要 Toonflow 仍给出明确问题清单和建议方案，就应重置当前节点的本地计数并继续该节点，不得因此结束整集流程。
3. Toonflow 仅回复“将派发”“现在开始”“请前往工作台”等承诺性文字，但在完整轮次结束后没有对应工具事件、工作区变化或后台任务时，不得视为节点完成，也不得从头重跑。控制器应针对当前节点发送一次明确的执行续令，要求实际调用既定工具；仍无证据时才暂停并报告。
4. 恢复或续令必须绑定当前 `projectId`、`scriptId`、阶段和最新工作区快照，只执行未完成节点。阶段 4 已达修复后 B 时可直接进入阶段 5；阶段 5 已写入部分轨道时只补缺失轨道；阶段 5.5 只补缺失或失败提示词。
5. `scriptPlan`、`storyboardTable` 等关键字段写入后必须执行完整性保护。若原本有效的长内容异常缩短为 `...`、空对象、占位文本或其他明显无效值，不得接受该快照、不得宣布完成；应通过 Toonflow 支持的执行轨恢复该字段，并确认未改动已验收的其他阶段数据。
6. 阶段 5 的完成证据必须来自真实 `addStoryboard` 写入结果：轨道数与分镜表片段数一致、全部 `videoDesc` 非空、全部 `shouldGenerateImage=0`。空 `generateStoryboard` 调用、自然语言确认或“面板已就绪”均不能替代数据验收。
7. 阶段 5.5 必须持续轮询工作台，允许已完成数暂时不增长；只要后端、Socket 和任务状态仍正常，就不得因短时无进度重启或重复提交。仅在官方超时、明确失败或连接异常时按第 5.6.6 节暂停。
8. 宣布完成前必须重新读取当前集工作区与视频工作台，而不是只信任本地状态文件。最终回读至少确认：`scriptPlan` 和 `storyboardTable` 有效、轨道总数大于 0、所有 `videoDesc` 非空、分镜图请求数为 0、最终提示词完成数等于轨道总数、空提示词/失败/生成中均为 0、状态为 `finished` 且 PID 为空。

## 6. 命令与验证边界

### 6.1 默认允许的只读命令

- `rg`、`rg --files`
- `Get-Content`、`Get-ChildItem`
- `git status`、`git diff`、`git log`、`git show`
- `yarn lint`（当前脚本仅执行 `tsc --noEmit`）
- 只读端口、进程和日志检查

### 6.2 默认禁止或需单独授权的命令

- `yarn build`：会覆盖 `data/serve/app.js` 和生成构建产物
- `yarn dist*`、`yarn pack`：会产生打包和发布产物
- `yarn vendor2json`：会生成供应商快照
- 数据库迁移、清库、导入、导出和直接 SQL
- 会自动格式化或重写整个仓库的命令
- 删除、重置、回滚、强制覆盖 Git 工作区的命令
- 发布、推送、创建 Release 或修改 CI/CD 的命令

启动服务不等于授权修改框架。若启动流程会自动重写 `src/router.ts`、`src/types/database.d.ts` 或其他受保护文件，应优先使用现有生产入口；启动后必须检查 Git 状态，发现框架差异立即停止并报告，不得擅自“修复”或提交这些差异。

## 7. 变更质量与交付要求

每次用户层修改完成后必须：

1. 用 `git diff -- <目标路径>` 检查只出现预期用户层变更。
2. 用 `git status --short` 确认没有系统框架文件被改动。
3. 检查 Markdown、JSON、YAML 或 TypeScript 定义的基本语法。
4. 能安全执行时运行 `yarn lint`；不得为了让检查通过而修改框架。
5. 向用户报告：改了哪些用户定义文件、行为变化、验证结果和仍需用户配置的事项。

如果发现任务开始前已有框架改动：保留原样，不覆盖、不回滚、不混入当前修改，并立即向用户说明。

## 8. 严禁事项

无论任务内容如何，除非满足第 4 节的精确框架授权，否则严禁：

- 修改 `src/` 中任何代码；
- 修改内置网页、Electron、构建、发布或数据库框架；
- 修改官方供应商适配器；
- 修改许可证、版权、品牌标识或官方 README；
- 把密钥写入仓库；
- 手工改数据库来伪造应用状态；
- 用生成命令覆盖受保护文件后将其作为用户层变更交付；
- 因为用户未明确反对就推定拥有框架写权限。

最终判断口诀：**白名单内按任务最小修改；白名单外只读；不确定就停止并报告。**
